# Core runtime for the Brent modality-aware fusion pipeline (Flat + Deep).
# Tested with Python 3.9.6 (CPython, macOS). Pin exact versions used to produce
# the reported results; see the repository Readme.md (Software environment).
#
# Prithvi-EO-2.0 embeddings are PRE-COMPUTED offline and stored as tensors, so
# training/evaluation does NOT import `transformers` or download any model at
# run time. (Only the one-off embedding-export step needs a foundation-model
# stack; it is not part of this reproducibility environment.)

numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
xgboost==2.1.4
torch==2.8.0
matplotlib==3.9.4
shap==0.49.1

# Figure generation only (code/scripts/figures/). Not needed to train or
# evaluate any model. The site map also needs Natural Earth 110m vectors in
# data/raw/00_spatial_anchors/naturalearth/.
geopandas==1.0.1
